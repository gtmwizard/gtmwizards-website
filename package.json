{
  "name": "gtmwizards-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "assets:dry": "node --env-file=.env scripts/upload-assets.mjs --dry-run",
    "assets:sync": "node --env-file=.env scripts/upload-assets.mjs",
    "check:dashes": "node scripts/check-dashes.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/instrument-sans": "^5.1.0",
    "@fontsource/ibm-plex-mono": "^5.1.0",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@cloudflare/workers-types": "^4.20241205.0",
    "mime-types": "^2.1.35"
  }
}
